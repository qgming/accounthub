# AccountHub

<div align="center">

**企业级多应用账户管理后台**

基于 Supabase 的集中式用户与会员管理系统

[![React](https://img.shields.io/badge/React-19-61dafb?logo=react)](https://react.dev/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.9-3178c6?logo=typescript)](https://www.typescriptlang.org/)
[![Vite](https://img.shields.io/badge/Vite-7-646cff?logo=vite)](https://vitejs.dev/)
[![Supabase](https://img.shields.io/badge/Supabase-Latest-3ecf8e?logo=supabase)](https://supabase.com/)
[![Ant Design](https://img.shields.io/badge/Ant%20Design-6.x-0170fe?logo=antdesign)](https://ant.design/)

</div>

---

## 📖 项目简介

AccountHub 是一个功能完善的 Supabase 应用管理后台，为多个应用程序提供统一的账户管理中心。通过单一界面，管理员可以高效管理所有关联应用的用户账户、会员订阅、支付记录和应用配置。

### 核心价值

- 🎯 **集中管理** — 一个后台管理所有应用的用户和会员数据
- 🔐 **权限分离** — 管理员与普通用户严格区分，安全可控
- 🔒 **配置加密** — 应用配置数据 AES-256-GCM 端到端加密存储
- 📊 **完整审计** — 所有管理操作自动记录，覆盖所有资源类型
- 🚀 **高性能** — 基于 React Query 的智能缓存和 React Compiler 优化
- 🎨 **企业级 UI** — Ant Design 6.x 组件库，专业美观

---

## ✨ 功能特性

### 👥 用户管理
- 查看和搜索所有应用的用户账户
- 编辑用户基本信息（昵称、头像、邮箱等）
- 封禁/解封用户账户
- 查看用户注册来源应用

### 💳 会员管理
- 管理用户会员状态（激活、未激活、已过期）
- 查看和编辑会员套餐信息
- 跟踪订阅周期和续费设置
- 支持手动续费管理

### 💰 支付管理
- 查看完整支付历史记录
- 支持多种支付方式（Stripe、支付宝、微信支付、易支付）
- 支付状态跟踪（成功、失败、待处理、已退款）
- 区分 `payment_history.status`（`success/failed/pending/refunded`）与会员支付状态

### 🎮 应用管理
- 注册和管理多个应用（自动生成 `app_key`）
- 应用版本控制和发布管理
- 应用配置模板系统（支持动态字段类型）
- 兑换码生成和管理（使用密码学安全随机数）
- 支付渠道配置管理

### 🔒 配置加密存储
- 配置数据在保存时使用关联应用的 `app_key` 加密（AES-256-GCM）
- 密钥派生使用 PBKDF2-SHA256（10 万次迭代），与 SDK 解密完全对称
- 编辑已加密配置时显示警告提示，保护数据安全

### 📝 审计日志
- 自动记录所有管理员操作（覆盖用户、会员、应用、兑换码等所有资源）
- 记录操作前后数据（`old_data` / `new_data`）
- IP 地址和 User Agent 追踪
- 可按时间、操作类型、资源类型筛选

---

## 🛠️ 技术栈

### 前端框架
- **React 19** — 支持 React Compiler 自动优化
- **TypeScript 5.9** — 严格模式，全面类型安全
- **Vite 7 (rolldown-vite)** — 极速构建

### UI 组件
- **Ant Design 6.x** — 企业级 UI 设计语言
- **Ant Design Icons** — 丰富的图标库
- **中文语言包** — 完整的中文本地化支持

### 状态管理
- **TanStack Query 5** — 服务端状态管理，智能缓存
- **React Context** — 全局认证状态

### 加密
- **@noble/ciphers** — AES-256-GCM 加密
- **@noble/hashes** — PBKDF2-SHA256 密钥派生

### 后端服务
- **Supabase** — PostgreSQL 数据库 + 认证

### 路由
- **React Router v7** — 声明式路由

---

## 🚀 快速开始

### 环境要求

- **Node.js** >= 18.0.0
- **Supabase 项目**（需配置相应的数据库表和权限）

### 安装步骤

1. **克隆项目**
```bash
git clone <repository-url>
cd accounthub
```

2. **安装依赖**
```bash
npm install
```

3. **配置环境变量**

创建 `.env.local` 文件：
```env
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

4. **启动开发服务器**
```bash
npm run dev
```

访问 `http://localhost:5173` 查看应用。

### 可用命令

```bash
npm run dev          # 开发模式（热重载）
npm run build        # 生产构建（tsc -b + vite build）
npm run preview      # 预览生产构建
npm run lint         # ESLint 代码检查
```

---

## 📁 项目结构

```
accounthub/
├── src/
│   ├── components/          # UI 组件
│   │   ├── applications/    # 应用管理
│   │   ├── appVersions/     # 版本管理
│   │   ├── appConfigs/      # 应用配置（含加密逻辑）
│   │   ├── users/           # 用户管理
│   │   ├── memberships/     # 会员管理
│   │   ├── payments/        # 支付管理
│   │   ├── redemptionCodes/ # 兑换码管理
│   │   ├── dashboard/       # 仪表盘
│   │   └── layout/          # 布局（Header、Sidebar）
│   ├── pages/               # 页面组件
│   ├── services/            # 服务层（数据操作 + 审计日志）
│   │   ├── audit.service.ts
│   │   ├── auth.service.ts
│   │   ├── users.service.ts
│   │   ├── applications.service.ts
│   │   ├── appVersions.service.ts
│   │   ├── appConfigs.service.ts
│   │   ├── memberships.service.ts
│   │   ├── payments.service.ts
│   │   └── redemptionCodes.service.ts
│   ├── hooks/               # React Query Hooks
│   ├── contexts/            # AuthContext
│   ├── types/               # TypeScript 类型（含数据库类型）
│   ├── utils/
│   │   ├── crypto.ts        # AES-256-GCM 加密工具
│   │   ├── validation.ts    # UUID 及输入验证
│   │   └── appkey.ts        # App Key 生成
│   ├── config/
│   │   ├── supabase.ts
│   │   ├── queryClient.ts
│   │   └── constants.ts     # 状态常量（含 PAYMENT_HISTORY_STATUS）
│   ├── App.tsx
│   └── main.tsx
├── .env.local              # 环境变量（需自行创建）
├── vite.config.ts
├── tsconfig.json
└── package.json
```

---

## 🏗️ 架构设计

### 分层架构

```
┌──────────────────────────────────────┐
│         UI Components (React)         │  ← Ant Design 6.x
├──────────────────────────────────────┤
│      Custom Hooks (TanStack Query)   │  ← 数据获取与缓存
├──────────────────────────────────────┤
│            Service Layer             │  ← 业务逻辑 + UUID 验证 + 审计
├──────────────────────────────────────┤
│           Supabase Client            │  ← PostgreSQL + Auth
└──────────────────────────────────────┘
```

### 核心设计模式

#### 服务层
- 封装所有 Supabase 操作
- 所有 ID 参数统一进行 UUID 格式验证（`utils/validation.ts`）
- 写操作（创建、更新、删除）自动记录审计日志

#### 审计日志
- 日志字段：`admin_id`、`action`、`resource_type`、`resource_id`、`old_data`、`new_data`、`ip_address`、`user_agent`
- 所有写操作均已覆盖：用户、会员、应用、兑换码

#### 配置加密
- 保存配置时，使用关联应用的 `app_key + app_id` 通过 PBKDF2 派生 AES-256-GCM 密钥
- 加密格式：`{ _enc: "enc:v1:<nonce>.<ciphertext+authTag>" }`
- 与 SDK 的解密实现完全对称，确保端到端安全

#### React Query 配置（`src/config/queryClient.ts`）
- 缓存时间：5 分钟
- GC 时间：10 分钟
- 窗口聚焦不自动重新获取
- 查询失败重试 1 次，变更不重试

---

## 🗄️ 数据库关键表

| 表名 | 说明 |
|------|------|
| `admins` | 管理员，可登录后台 |
| `users` | 普通用户，不可登录后台 |
| `applications` | 注册的应用（含 `app_key`） |
| `user_app_memberships` | 用户会员关系，状态：`active / inactive / expired` |
| `payment_history` | 支付历史，状态：`success / failed / pending / refunded` |
| `app_versions` | 应用版本（无 `platform` 字段） |
| `app_configs` | 应用配置（`config_data` 支持加密存储） |
| `redemption_codes` | 兑换码 |
| `admin_audit_logs` | 审计日志 |

---

## 🔐 权限与安全

### 管理员 vs 普通用户
- **管理员（Admins）**：存储在 `admins` 表，可登录后台
- **普通用户（Users）**：存储在 `users` 表，不可登录后台，两表职责严格分离

### 安全措施
- 所有 ID 参数强制 UUID 格式验证，防止注入
- 配置数据 AES-256-GCM 端到端加密
- 所有写操作记录审计日志，操作可追溯
- Supabase Row Level Security (RLS) 策略

---

## 🔧 配置说明

### 环境变量

| 变量名 | 说明 | 必需 |
|--------|------|------|
| `VITE_SUPABASE_URL` | Supabase 项目 URL | ✅ |
| `VITE_SUPABASE_ANON_KEY` | Supabase 匿名密钥 | ✅ |

---

## 📚 开发指南

### 添加新功能

1. **创建服务模块**（`src/services/`）
   - 添加 UUID 输入验证
   - 写操作添加审计日志调用
   - 定义自定义错误类型

2. **创建 React Query Hook**（`src/hooks/`）
   - `useQuery` 获取数据，`useMutation` 修改数据
   - 配置缓存失效策略

3. **创建 UI 组件**（`src/components/`）

4. **添加路由**（`src/App.tsx`）

### 代码规范

- TypeScript 严格模式，运行 `tsc -b` 检查类型
- 遵循 ESLint 规则，提交前运行 `npm run lint`
- 服务层函数使用 async/await，写操作包含审计日志

---

## 🐛 故障排除

**Q: 无法连接到 Supabase**
- 检查 `.env.local` 配置是否正确
- 确认 URL 和 Key 有效，检查网络连接

**Q: 登录后立即退出**
- 确认用户在 `admins` 表中存在
- 检查 Supabase Auth 用户状态

**Q: 配置数据显示 `{ _enc: "..." }` 而非实际内容**
- 这是正常的加密存储格式，SDK 端会自动解密
- 确认 SDK 初始化时传入的 `appKey` 与应用的 `app_key` 一致

**Q: TypeScript 类型错误**
- 运行 `tsc -b` 查看详细错误
- 确保 `database.types.ts` 与数据库架构同步

---

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支（`git checkout -b feature/AmazingFeature`）
3. 提交更改（`git commit -m 'feat: add AmazingFeature'`）
4. 推送到分支（`git push origin feature/AmazingFeature`）
5. 开启 Pull Request

### 提交规范

- `feat:` 新功能
- `fix:` 错误修复
- `docs:` 文档更新
- `refactor:` 代码重构
- `chore:` 构建/工具链更新

---

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

---

## 🔗 相关链接

- [AccountHub SDK](https://github.com/qgming/accounthubsdk) — 客户端 SDK
- [Supabase 文档](https://supabase.com/docs)

---

<div align="center">

**使用 TypeScript 构建**

</div>
