# AccountHub

<div align="center">

**企业级多应用账户管理后台**

基于 Supabase 的集中式用户与会员管理系统

[![React](https://img.shields.io/badge/React-19-61dafb?logo=react)](https://react.dev/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.6-3178c6?logo=typescript)](https://www.typescriptlang.org/)
[![Vite](https://img.shields.io/badge/Vite-6.0-646cff?logo=vite)](https://vitejs.dev/)
[![Supabase](https://img.shields.io/badge/Supabase-Latest-3ecf8e?logo=supabase)](https://supabase.com/)

</div>

---

## 📖 项目简介

AccountHub 是一个功能完善的 Supabase 应用管理后台，为多个应用程序提供统一的账户管理中心。通过单一界面，管理员可以高效管理所有关联应用的用户账户、会员订阅、支付记录和应用配置。

### 核心价值

- 🎯 **集中管理**：一个后台管理所有应用的用户和会员数据
- 🔐 **权限分离**：管理员与普通用户严格区分，安全可控
- 📊 **完整审计**：所有管理操作自动记录，可追溯可审查
- 🚀 **高性能**：基于 React Query 的智能缓存和 React Compiler 优化
- 🎨 **企业级 UI**：Ant Design 组件库，专业美观

---

## ✨ 功能特性

### 👥 用户管理
- 查看和搜索所有应用的用户账户
- 编辑用户基本信息（昵称、头像、邮箱等）
- 封禁/解封用户账户
- 查看用户注册来源应用
- 批量操作和数据导出

### 💳 会员管理
- 管理用户会员状态（激活、暂停、过期等）
- 查看和编辑会员套餐信息
- 跟踪订阅周期和自动续费设置
- 支持试用期和手动续费管理
- 会员等级和权益配置

### 💰 支付管理
- 查看完整支付历史记录
- 支持多种支付方式（Stripe、支付宝、微信支付）
- 支付状态跟踪（成功、失败、待处理、已退款）
- 退款处理和记录

### 🎮 应用管理
- 注册和管理多个应用
- 应用版本控制和发布管理
- 应用配置模板系统
- 兑换码生成和管理
- 支付配置管理

### 📝 审计日志
- 自动记录所有管理员操作
- 详细的操作前后数据对比
- IP 地址和用户代理追踪
- 可按时间、操作类型、资源类型筛选

---

## 🛠️ 技术栈

### 前端框架
- **React 19** - 最新的 React 版本，支持 React Compiler
- **TypeScript 5.6** - 完整的类型安全保障
- **Vite 6.0** - 极速的开发体验和构建性能

### UI 组件
- **Ant Design 5.x** - 企业级 UI 设计语言
- **Ant Design Icons** - 丰富的图标库
- **中文语言包** - 完整的中文本地化支持

### 状态管理
- **TanStack Query (React Query)** - 强大的服务端状态管理
- **React Context** - 全局认证状态管理

### 后端服务
- **Supabase** - PostgreSQL 数据库 + 认证 + 实时订阅
- **Supabase Auth** - 内置的用户认证系统

### 路由
- **React Router v7** - 声明式路由管理

### 开发工具
- **ESLint** - 代码质量检查
- **React Compiler** - 自动性能优化
- **TypeScript Compiler** - 类型检查

---

## 🚀 快速开始

### 环境要求

- **Node.js** >= 18.0.0
- **npm** >= 9.0.0
- **Supabase 项目**（需要配置相应的数据库表和权限）

### 安装步骤

1. **克隆项目**
```bash
git clone <repository-url>
cd accounthub
```

2. **安装依赖**
```bash
npm install
```

3. **配置环境变量**

创建 `.env.local` 文件：
```env
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

4. **启动开发服务器**
```bash
npm run dev
```

访问 `http://localhost:5173` 查看应用。

### 可用命令

```bash
# 开发模式（热重载）
npm run dev

# 生产构建
npm run build

# 预览生产构建
npm run preview

# 代码检查
npm run lint

# TypeScript 类型检查
npm run type-check
```

---

## 📁 项目结构

```
accounthub/
├── src/
│   ├── components/          # UI 组件
│   │   ├── applications/    # 应用管理组件
│   │   ├── appVersions/     # 应用版本组件
│   │   ├── appConfigs/      # 应用配置组件
│   │   ├── users/           # 用户管理组件
│   │   ├── memberships/     # 会员管理组件
│   │   ├── payments/        # 支付管理组件
│   │   ├── redemptionCodes/ # 兑换码组件
│   │   ├── layout/          # 布局组件（Header、Sidebar）
│   │   └── common/          # 通用组件
│   ├── pages/               # 页面组件
│   │   ├── LoginPage.tsx
│   │   ├── UsersPage.tsx
│   │   ├── MembershipsPage.tsx
│   │   └── ...
│   ├── services/            # 服务层（数据操作）
│   │   ├── auth.service.ts
│   │   ├── users.service.ts
│   │   ├── memberships.service.ts
│   │   ├── applications.service.ts
│   │   └── audit.service.ts
│   ├── hooks/               # 自定义 React Hooks
│   │   ├── useUsers.ts
│   │   ├── useMemberships.ts
│   │   └── useApplications.ts
│   ├── contexts/            # React Context
│   │   └── AuthContext.tsx
│   ├── types/               # TypeScript 类型定义
│   │   └── database.types.ts
│   ├── utils/               # 工具函数
│   │   └── validation.ts
│   ├── config/              # 配置文件
│   │   ├── supabase.ts
│   │   ├── queryClient.ts
│   │   └── constants.ts
│   ├── App.tsx              # 根组件
│   └── main.tsx             # 应用入口
├── public/                  # 静态资源
├── .env.local              # 环境变量（需自行创建）
├── CLAUDE.md               # AI 助手项目指南
├── vite.config.ts          # Vite 配置
├── tsconfig.json           # TypeScript 配置
└── package.json            # 项目依赖
```

---

## 🏗️ 架构设计

### 分层架构

```
┌─────────────────────────────────────┐
│         UI Components (React)        │  ← Ant Design 组件
├─────────────────────────────────────┤
│      Custom Hooks (React Query)     │  ← 数据获取和缓存
├─────────────────────────────────────┤
│         Service Layer               │  ← 业务逻辑封装
├─────────────────────────────────────┤
│         Supabase Client             │  ← 数据库操作
└─────────────────────────────────────┘
```

### 核心设计模式

#### 1. 服务层模式
所有数据操作通过 `src/services/` 中的服务模块进行：
- 封装 Supabase 客户端调用
- 统一的输入验证（使用 `utils/validation.ts`）
- 自定义错误类（`ValidationError`、`AuditError` 等）
- 业务逻辑集中管理

#### 2. React Query Hooks 模式
`src/hooks/` 中的自定义 Hooks 将服务层与 React Query 结合：
- `useQuery` 用于数据获取，自动缓存
- `useMutation` 用于数据修改，支持乐观更新
- 自动的缓存失效和重新获取
- Ant Design 消息通知集成

#### 3. 认证流程
- `AuthContext` 提供全局认证状态
- 区分 `User`（普通用户，不能登录）和 `Admin`（管理员，可登录后台）
- `ProtectedRoute` 组件保护管理员路由
- Supabase Session 持久化

#### 4. 审计日志
- 所有管理操作自动记录到 `admin_audit_logs` 表
- 使用 `auditService.logAction()` 创建审计记录
- 记录操作类型、资源、修改前后数据、IP 地址等

---

## 🗄️ 数据库架构

### 核心数据表

#### `admins` - 管理员表
管理员用户，可以登录此后台
- `auth_user_id` - 关联 Supabase Auth 用户
- 与 `users` 表分离

#### `users` - 普通用户表
应用的普通用户，不能登录后台
- `is_banned` - 封禁标记
- `registered_from_app_id` - 注册来源应用
- **注意**：`profiles` 表已废弃，请使用 `users` 表

#### `applications` - 应用表
注册的应用程序
- `app_key` - API 认证密钥
- `slug` - URL 友好标识符
- `is_active` - 激活状态

#### `user_app_memberships` - 用户会员关系表
关联用户与应用的会员状态
- 状态：active、inactive、suspended、expired、trial、cancelled
- 支付状态：paid、pending、failed、refunded
- 支持试用期和自动续费

#### `subscriptions` - 订阅表
订阅计费信息
- 关联到 `user_app_memberships`
- 支持多种支付方式：stripe、alipay、wechat、manual
- 跟踪计费周期和下次计费日期

#### `payment_history` - 支付历史表
支付交易记录
- 状态：success、failed、pending、refunded
- 关联到订阅和用户

#### `admin_audit_logs` - 审计日志表
记录所有管理员操作
- 操作类型、资源、修改前后数据
- IP 地址和 User Agent

---

## 🔐 权限与安全

### 管理员 vs 普通用户
- **管理员（Admins）**：存储在 `admins` 表，可以登录此后台
- **普通用户（Users）**：存储在 `users` 表，不能登录此后台
- 两者职责明确分离，互不混淆

### 安全措施
- 所有管理操作需要管理员认证
- 输入验证防止 SQL 注入和 XSS
- UUID 格式验证
- 审计日志记录所有敏感操作
- Supabase Row Level Security (RLS) 策略

---

## 🎨 UI/UX 特性

- **响应式设计**：适配桌面和平板设备
- **中文界面**：完整的中文本地化
- **暗色主题**：支持深色模式（可扩展）
- **表格操作**：搜索、筛选、排序、分页
- **表单验证**：实时验证和错误提示
- **加载状态**：优雅的加载动画和骨架屏
- **消息通知**：操作成功/失败的即时反馈

---

## 🔧 配置说明

### 环境变量

| 变量名 | 说明 | 必需 |
|--------|------|------|
| `VITE_SUPABASE_URL` | Supabase 项目 URL | ✅ |
| `VITE_SUPABASE_ANON_KEY` | Supabase 匿名密钥 | ✅ |

### React Query 配置
位于 `src/config/queryClient.ts`：
- 缓存时间：5 分钟
- 垃圾回收时间：10 分钟
- 窗口聚焦时不自动重新获取
- 查询失败重试 1 次，变更不重试

---

## 📚 开发指南

### 添加新功能

1. **创建服务模块**（`src/services/`）
   - 封装数据库操作
   - 添加输入验证
   - 定义自定义错误类

2. **创建 React Query Hook**（`src/hooks/`）
   - 使用 `useQuery` 获取数据
   - 使用 `useMutation` 修改数据
   - 配置缓存失效策略

3. **创建 UI 组件**（`src/components/`）
   - 使用 Ant Design 组件
   - 调用自定义 Hook
   - 添加加载和错误状态

4. **添加路由**（`src/App.tsx`）
   - 在 `ProtectedRoute` 下添加新路由
   - 更新侧边栏导航

### 代码规范

- 使用 TypeScript 严格模式
- 遵循 ESLint 规则
- 组件使用函数式组件和 Hooks
- 服务层函数使用 async/await
- 错误处理使用 try-catch
- 提交前运行 `npm run lint` 和 `npm run type-check`

### 类型安全

`src/types/database.types.ts` 提供完整的类型定义：
- `Row` - SELECT 查询类型
- `Insert` - INSERT 操作类型（省略自动生成字段）
- `Update` - UPDATE 操作类型（所有字段可选）

---

## 🐛 故障排除

### 常见问题

**Q: 无法连接到 Supabase**
- 检查 `.env.local` 文件是否正确配置
- 确认 Supabase URL 和 Key 有效
- 检查网络连接

**Q: 登录后立即退出**
- 确认用户在 `admins` 表中存在
- 检查 Supabase Auth 用户状态
- 查看浏览器控制台错误信息

**Q: 数据不更新**
- React Query 可能使用了缓存数据
- 手动刷新页面或清除缓存
- 检查 `queryClient.ts` 配置

**Q: TypeScript 类型错误**
- 运行 `npm run type-check` 查看详细错误
- 确保 `database.types.ts` 与数据库架构同步
- 更新依赖包到最新版本

---

## 🤝 贡献指南

欢迎贡献代码、报告问题或提出建议！

### 提交流程

1. Fork 本仓库
2. 创建特性分支（`git checkout -b feature/AmazingFeature`）
3. 提交更改（`git commit -m 'Add some AmazingFeature'`）
4. 推送到分支（`git push origin feature/AmazingFeature`）
5. 开启 Pull Request

### 提交规范

- `feat:` 新功能
- `fix:` 错误修复
- `docs:` 文档更新
- `style:` 代码格式调整
- `refactor:` 代码重构
- `test:` 测试相关
- `chore:` 构建/工具链更新

---

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

---

## 📞 联系方式

如有问题或建议，请通过以下方式联系：

- 提交 Issue
- 发起 Discussion
- 邮件联系：[your-email@example.com]

---

<div align="center">

**使用 ❤️ 和 TypeScript 构建**

</div>
